{
  "devDependencies": {
    "@cucumber/cucumber": "^12.7.0",
    "allure-cucumberjs": "^3.6.0",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.2"
  },
  "type": "commonjs",
  "scripts": {
    "clean:allure": "rmdir /s /q allure-results & rmdir /s /q allure-report",
   "test:bdd": "node --require ts-node/register ./node_modules/@cucumber/cucumber/bin/cucumber-js tests/bdd/features --format allure-cucumberjs/reporter",
    "report:generate": "npx allure-commandline generate allure-results --clean -o allure-report",
    "test:report": "npm run clean:allure && npm run test:bdd && npm run report:generate",
    "test:allure": "npm run clean:allure & npm run test:bdd & npx allure-commandline generate --single-file allure-results --clean -o allure-report & start \"\" \"allure-report\\index.html\""
  }
}
